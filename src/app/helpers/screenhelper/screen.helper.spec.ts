import { TestBed, inject } from '@angular/core/testing';

import { ScreenHelper } from './screen.helper';

describe('ScreenHelper', () => {
    beforeEach(() => {
        TestBed.configureTestingModule({});
    });

    it('will check is mobile size', () => {
        
    })
});
