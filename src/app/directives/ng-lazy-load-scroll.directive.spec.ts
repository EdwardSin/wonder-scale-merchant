import { NgLazyLoadScrollDirective } from './ng-lazy-load-scroll.directive';

describe('NgLazyLoadScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new NgLazyLoadScrollDirective();
    expect(directive).toBeTruthy();
  });
});
