import { WsEmptyDashPipe } from './ws-empty-dash.pipe';

describe('WsEmptyDashPipe', () => {
  it('create an instance', () => {
    const pipe = new WsEmptyDashPipe();
    expect(pipe).toBeTruthy();
  });
});
