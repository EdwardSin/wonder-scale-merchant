import { WsPassDatePipe } from './ws-pass-date.pipe';

describe('WsPassDatePipe', () => {
  it('create an instance', () => {
    const pipe = new WsPassDatePipe();
    expect(pipe).toBeTruthy();
  });
});
