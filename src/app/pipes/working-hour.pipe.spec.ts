import { WorkingHourPipe } from './working-hour.pipe';

describe('WorkingHourPipe', () => {
  it('create an instance', () => {
    const pipe = new WorkingHourPipe();
    expect(pipe).toBeTruthy();
  });
});
