import { WsDateCountDownPipe } from './ws-date-count-down.pipe';

describe('WsDateCountDownPipe', () => {
  it('create an instance', () => {
    const pipe = new WsDateCountDownPipe();
    expect(pipe).toBeTruthy();
  });
});
