import { WsCurrencyPipe } from './ws-currency.pipe';

describe('WsCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new WsCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
